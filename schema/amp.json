{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/agent-memory-protocol/amp/schema/amp.json",
  "title": "Agent Memory Protocol (AMP) v1.1 — Tool Schemas",
  "description": "JSON Schema definitions for all AMP tool inputs and outputs.",
  "definitions": {
    "Scope": {
      "type": "object",
      "description": "Multi-dimensional partition keys for scoping memory. v1.1 standard.",
      "properties": {
        "agent_id": {
          "type": "string",
          "description": "Logical identity of the agent."
        },
        "group_id": {
          "type": "string",
          "description": "Collaborative group or team identifier."
        },
        "workspace_id": {
          "type": "string",
          "description": "Shared workspace identifier."
        },
        "user_id": {
          "type": "string",
          "description": "The end-user interacting with the agent."
        },
        "session_id": {
          "type": "string",
          "description": "The specific conversation turn, flow, or run (alias: run_id)."
        },
        "app_id": {
          "type": "string",
          "description": "Specific product surface, app instance, or platform."
        },
        "org_id": {
          "type": "string",
          "description": "Tenant partition."
        }
      },
      "anyOf": [
        { "required": ["agent_id"] },
        { "required": ["group_id"] },
        { "required": ["workspace_id"] },
        { "required": ["user_id"] }
      ],
      "additionalProperties": false
    },
    "MemoryResult": {
      "type": "object",
      "description": "Canonical memory object returned in recall responses.",
      "required": ["id", "content", "score", "timestamp", "status", "scope"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Backend-assigned unique identifier."
        },
        "content": {
          "type": "string",
          "description": "Text content of the memory."
        },
        "score": {
          "type": "number",
          "description": "Relevance score for the query. 0.0–1.0 recommended; backend may use own scale."
        },
        "source": {
          "type": "string",
          "description": "Provenance label (e.g. user_stated, inferred, external)."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 creation time."
        },
        "status": {
          "type": "string",
          "enum": ["active", "pinned", "archived"],
          "description": "Current lifecycle status of the memory."
        },
        "scope": {
          "$ref": "#/definitions/Scope"
        },
        "visibility": {
          "type": "string",
          "enum": ["private", "shared"],
          "deprecated": true,
          "description": "Deprecated in favor of multi-dimensional scoping in v1.1."
        },
        "metadata": {
          "type": "object",
          "description": "Backend-specific fields. Supports reserved AMP v1.1 metadata keys.",
          "properties": {
            "amp.ttl": {
              "type": "integer",
              "minimum": 0,
              "description": "Time-to-live in seconds from creation."
            },
            "amp.confidence": {
              "type": "number",
              "minimum": 0.0,
              "maximum": 1.0,
              "description": "System or model confidence score in the validity of the memory."
            },
            "amp.entities": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Extracted entities related to this memory."
            },
            "amp.relations": {
              "type": "array",
              "items": {
                "type": "object",
                "required": ["subject", "predicate", "object"],
                "properties": {
                  "subject": { "type": "string" },
                  "predicate": { "type": "string" },
                  "object": { "type": "string" }
                }
              },
              "description": "Subject-Predicate-Object triplets for graph-based stores."
            },
            "amp.categories": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Broad classification categories for organizing memories."
            },
            "amp.summary": {
              "type": "string",
              "description": "A higher-order summarized representation of this memory."
            }
          },
          "additionalProperties": true
        }
      }
    },
    "RecallFilters": {
      "type": "object",
      "description": "Optional filters for amp.recall.",
      "properties": {
        "status": {
          "type": "string",
          "enum": ["active", "pinned", "archived"],
          "description": "Filter by memory status. If absent, backends SHOULD return active and pinned memories."
        },
        "visibility": {
          "type": "string",
          "enum": ["private", "shared"],
          "deprecated": true,
          "description": "Deprecated. Filter by visibility."
        },
        "source": {
          "type": "string",
          "description": "Filter by provenance label."
        },
        "timestamp_after": {
          "type": "string",
          "format": "date-time",
          "description": "Return only memories created after this time."
        },
        "timestamp_before": {
          "type": "string",
          "format": "date-time",
          "description": "Return only memories created before this time."
        },
        "metadata_filters": {
          "type": "array",
          "description": "v1.2-draft. Structured predicates over the memory's metadata bag. All entries are combined with strict AND -- a memory MUST satisfy every filter to be returned. Backends MUST treat missing keys and mismatched value types as a filter miss (not a server error). See spec section 3.2.2 for the operator contract.",
          "items": { "$ref": "#/definitions/MetadataFilter" }
        }
      },
      "additionalProperties": false
    },
    "MetadataFilter": {
      "type": "object",
      "description": "v1.2-draft. A single key-operator-value predicate over a memory's metadata bag. Used inside RecallFilters.metadata_filters[]. Keys are FLAT lookups against the stored metadata object -- dotted strings like 'amp.confidence' are literal keys, not path expressions (a future revision may add path support behind an explicit flag).",
      "required": ["key", "operator", "value"],
      "additionalProperties": false,
      "properties": {
        "key": {
          "type": "string",
          "description": "Metadata key to evaluate (flat lookup against the stored metadata object). Reserved AMP keys like 'amp.confidence' or 'amp.categories' are addressable directly."
        },
        "operator": {
          "type": "string",
          "enum": ["eq", "ne", "gt", "gte", "lt", "lte", "in", "contains"],
          "description": "Comparison operator. Per-operator value type: eq/ne/gt/gte/lt/lte expect a scalar (string|number|boolean). 'in' expects an array; the row matches when the stored value equals any element. 'contains' expects a scalar; the row matches when the stored value (array OR string) contains that scalar. Backends MUST return invalid_request for any other operator string."
        },
        "value": {
          "description": "Comparison value. Type depends on operator (see operator description). Strings, numbers, booleans, and arrays-of-scalars are the supported shapes; arbitrary nested objects are reserved for a future revision."
        }
      }
    },
    "AmpErrorData": {
      "type": "object",
      "description": "AMP error payload placed in the JSON-RPC error data field.",
      "required": ["amp_error_code", "message"],
      "properties": {
        "amp_error_code": {
          "type": "string",
          "enum": ["invalid_request", "not_found", "not_supported", "backend_error"],
          "description": "AMP-specific error code."
        },
        "message": {
          "type": "string",
          "description": "Human-readable error description."
        }
      }
    },
    "ToolAnnotations": {
      "type": "object",
      "description": "MCP 2025-03-26 tool annotations. Hints to hosts about the tool's behaviour so they can apply appropriate gating (e.g. require user confirmation for destructive verbs, cache results from read-only ones). Per the MCP spec these are HINTS, not guarantees -- backends MUST still enforce real semantics server-side.",
      "additionalProperties": false,
      "properties": {
        "title": {
          "type": "string",
          "description": "Human-readable display name for UIs that surface tool catalogs."
        },
        "readOnlyHint": {
          "type": "boolean",
          "description": "true if the tool does not modify any backend state. Recall and stats are readOnly; encode/forget/consolidate/pin/import are not."
        },
        "destructiveHint": {
          "type": "boolean",
          "description": "true if the tool can delete or destroy data. Only amp.forget is destructive; pin/consolidate/encode merely add or annotate."
        },
        "idempotentHint": {
          "type": "boolean",
          "description": "true if calling the tool repeatedly with the same arguments produces the same effect as calling it once. recall/forget/pin/stats are idempotent; encode is not (each call produces a new memory id); consolidate is not (each pass mutates differently)."
        },
        "openWorldHint": {
          "type": "boolean",
          "description": "true if the tool's outputs depend on state outside the immediate scope/partition -- e.g. clock time, external services, cross-partition graph state. amp.consolidate is openWorld (mutates an evolving graph and may pull external embeddings)."
        }
      }
    }
  },
  "tools": {
    "amp.encode": {
      "description": "Store a new memory for an agent.",
      "annotations": {
        "title": "Encode Memory",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "input": {
        "type": "object",
        "required": ["content"],
        "anyOf": [
          { "required": ["scope"] },
          { "required": ["agent_id"] }
        ],
        "properties": {
          "scope": {
            "$ref": "#/definitions/Scope"
          },
          "agent_id": {
            "type": "string",
            "description": "Legacy namespace identifier for the calling agent (mapped to scope.agent_id)."
          },
          "private": {
            "type": "boolean",
            "default": false,
            "deprecated": true,
            "description": "Deprecated in favor of multi-dimensional scoping."
          },
          "content": {
            "type": "string",
            "minLength": 1,
            "description": "Text content to store as a memory."
          },
          "source": {
            "type": "string",
            "description": "Provenance label (e.g. user_stated, inferred, external)."
          },
          "force": {
            "type": "boolean",
            "default": false,
            "description": "If true, bypass salience threshold and store unconditionally."
          },
          "metadata": {
            "type": "object",
            "description": "Hints to the backend (e.g. salience, modality). Supports reserved AMP keys.",
            "additionalProperties": true
          }
        },
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "required": ["status"],
        "properties": {
          "id": {
            "type": "string",
            "description": "New memory identifier. Present when status is 'stored', 'duplicate', or 'queued'."
          },
          "status": {
            "type": "string",
            "enum": ["stored", "duplicate", "below_threshold", "queued"],
            "description": "Outcome of the encode operation."
          },
          "visibility": {
            "type": "string",
            "enum": ["private", "shared"],
            "deprecated": true,
            "description": "Deprecated. Echoes back memory visibility."
          },
          "event_id": {
            "type": "string",
            "description": "Optional async processing/transaction reference for status='queued'."
          }
        }
      }
    },
    "amp.batch_encode": {
      "description": "Store multiple memories in a single network round-trip. All entries are encoded under one shared scope; per-entry failures are reported in the response rather than aborting the batch. Introduced in v1.2-draft.",
      "annotations": {
        "title": "Batch Encode Memories",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "input": {
        "type": "object",
        "required": ["entries"],
        "anyOf": [
          { "required": ["scope"] },
          { "required": ["agent_id"] }
        ],
        "properties": {
          "scope": {
            "$ref": "#/definitions/Scope"
          },
          "agent_id": {
            "type": "string",
            "description": "Legacy namespace identifier (mapped to scope.agent_id)."
          },
          "entries": {
            "type": "array",
            "minItems": 0,
            "maxItems": 1000,
            "description": "Ordered list of memories to encode. Each entry has the same shape as amp.encode input minus the scope keys (which are taken from the top-level scope). Backends MAY enforce a smaller maxItems and MUST reject over-limit requests with invalid_request.",
            "items": {
              "type": "object",
              "required": ["content"],
              "additionalProperties": false,
              "properties": {
                "content": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Text content to store. Empty strings are rejected per-row (status=invalid_request on that row)."
                },
                "source": {
                  "type": "string",
                  "description": "Provenance label (e.g. user_stated, inferred, external)."
                },
                "force": {
                  "type": "boolean",
                  "default": false,
                  "description": "If true, bypass salience threshold for THIS row only."
                },
                "metadata": {
                  "type": "object",
                  "additionalProperties": true,
                  "description": "Per-row metadata patch applied to the new memory. Supports reserved AMP keys."
                }
              }
            }
          }
        },
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "required": ["results"],
        "properties": {
          "results": {
            "type": "array",
            "description": "Per-row outcomes in the SAME ORDER as the input entries[] array. Length is always equal to entries.length so callers can correlate by index.",
            "items": {
              "type": "object",
              "required": ["status"],
              "properties": {
                "id": {
                  "type": "string",
                  "description": "New memory identifier. Present when status is 'stored', 'duplicate', or 'queued'."
                },
                "status": {
                  "type": "string",
                  "enum": ["stored", "duplicate", "below_threshold", "queued", "invalid_request", "backend_error"],
                  "description": "Outcome for this row. 'invalid_request' and 'backend_error' are row-level mirrors of the corresponding amp_error_code values from spec section 3.5; the request itself succeeds (HTTP 200 / no JSON-RPC error) even when some rows have these statuses. Request-level errors (no scope, malformed entries array) raise a normal AmpError frame."
                },
                "message": {
                  "type": "string",
                  "description": "Human-readable failure detail. Present when status is 'invalid_request' or 'backend_error'."
                }
              }
            }
          },
          "summary": {
            "type": "object",
            "description": "Aggregate counts across the batch. Sum of all four fields equals results.length.",
            "required": ["stored", "below_threshold", "duplicate", "failed"],
            "properties": {
              "stored": { "type": "integer", "minimum": 0 },
              "below_threshold": { "type": "integer", "minimum": 0 },
              "duplicate": { "type": "integer", "minimum": 0 },
              "failed": { "type": "integer", "minimum": 0, "description": "Sum of rows with status 'invalid_request' or 'backend_error'." }
            }
          }
        }
      }
    },
    "amp.recall": {
      "description": "Retrieve memories relevant to a query.",
      "annotations": {
        "title": "Recall Memories",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "input": {
        "type": "object",
        "required": ["query"],
        "anyOf": [
          { "required": ["scope"] },
          { "required": ["agent_id"] }
        ],
        "properties": {
          "scope": {
            "$ref": "#/definitions/Scope"
          },
          "agent_id": {
            "type": "string",
            "description": "Legacy namespace identifier for the calling agent (mapped to scope.agent_id)."
          },
          "query": {
            "type": "string",
            "minLength": 1,
            "description": "Natural language or keyword query."
          },
          "top_k": {
            "type": "integer",
            "minimum": 1,
            "default": 10,
            "description": "Maximum number of results to return."
          },
          "filters": {
            "$ref": "#/definitions/RecallFilters"
          }
        },
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "required": ["results"],
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/definitions/MemoryResult"
            },
            "description": "Memories ordered by descending relevance score."
          }
        }
      }
    },
    "amp.forget": {
      "description": "Permanently delete a memory.",
      "annotations": {
        "title": "Forget Memory",
        "readOnlyHint": false,
        "destructiveHint": true,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "input": {
        "type": "object",
        "required": ["id"],
        "anyOf": [
          { "required": ["scope"] },
          { "required": ["agent_id"] }
        ],
        "properties": {
          "scope": {
            "$ref": "#/definitions/Scope"
          },
          "agent_id": {
            "type": "string",
            "description": "Legacy namespace identifier for the calling agent (mapped to scope.agent_id)."
          },
          "id": {
            "type": "string",
            "description": "ID of the memory to delete."
          }
        },
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "required": ["status"],
        "properties": {
          "status": {
            "type": "string",
            "enum": ["forgotten", "not_found"],
            "description": "Outcome of the forget operation."
          }
        }
      }
    },
    "amp.consolidate": {
      "description": "Trigger backend consolidation. Full conformance only.",
      "annotations": {
        "title": "Consolidate Memories",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      },
      "input": {
        "type": "object",
        "anyOf": [
          { "required": ["scope"] },
          { "required": ["agent_id"] }
        ],
        "properties": {
          "scope": {
            "$ref": "#/definitions/Scope"
          },
          "agent_id": {
            "type": "string",
            "description": "Legacy namespace identifier for the calling agent (mapped to scope.agent_id)."
          },
          "depth": {
            "type": "string",
            "enum": ["full", "light"],
            "default": "full",
            "description": "Consolidation depth. 'full' = complete pass; 'light' = minimal/incremental pass."
          }
        },
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "required": ["status"],
        "properties": {
          "status": {
            "type": "string",
            "enum": ["queued", "ok", "not_supported"],
            "description": "Outcome of the consolidate operation."
          },
          "memories_processed": {
            "type": "integer",
            "minimum": 0,
            "description": "Number of memories processed. Present when status is 'ok'."
          }
        }
      }
    },
    "amp.pin": {
      "description": "Mark a memory as permanent (never archived by consolidation). Full conformance only.",
      "annotations": {
        "title": "Pin Memory",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "input": {
        "type": "object",
        "required": ["id"],
        "anyOf": [
          { "required": ["scope"] },
          { "required": ["agent_id"] }
        ],
        "properties": {
          "scope": {
            "$ref": "#/definitions/Scope"
          },
          "agent_id": {
            "type": "string",
            "description": "Legacy namespace identifier for the calling agent (mapped to scope.agent_id)."
          },
          "id": {
            "type": "string",
            "description": "ID of the memory to pin."
          }
        },
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "required": ["status"],
        "properties": {
          "status": {
            "type": "string",
            "enum": ["pinned", "not_found", "not_supported"],
            "description": "Outcome of the pin operation."
          }
        }
      }
    },
    "amp.update": {
      "description": "Mutate the content and/or metadata of an existing memory in place. The memory's id, scope, source, status, and creation_time are preserved; only content and metadata are mutable. Introduced in v1.2-draft.",
      "annotations": {
        "title": "Update Memory",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "input": {
        "type": "object",
        "required": ["id"],
        "anyOf": [
          { "required": ["scope"] },
          { "required": ["agent_id"] }
        ],
        "properties": {
          "scope": {
            "$ref": "#/definitions/Scope"
          },
          "agent_id": {
            "type": "string",
            "description": "Legacy namespace identifier for the calling agent (mapped to scope.agent_id)."
          },
          "id": {
            "type": "string",
            "description": "ID of the memory to update."
          },
          "content": {
            "type": "string",
            "minLength": 1,
            "description": "Replacement content. Omit to leave content unchanged. Empty strings are NOT a delete signal — they are rejected with invalid_request (a memory cannot have empty content)."
          },
          "metadata": {
            "type": "object",
            "additionalProperties": true,
            "description": "Metadata patch. By default applied as a JSON Merge Patch (RFC 7396): keys present in the request are written into the stored metadata bag, keys absent are preserved, and a key whose value is explicit JSON null is removed. Backends MAY offer a 'replace' mode by accepting metadata_mode='replace' (see below)."
          },
          "metadata_mode": {
            "type": "string",
            "enum": ["merge", "replace"],
            "default": "merge",
            "description": "How `metadata` is applied. merge (default) — RFC 7396 JSON Merge Patch semantics. replace — the stored metadata bag is replaced wholesale with the request value. The default is merge because it matches the principle of least surprise for a partial-update verb."
          }
        },
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "required": ["status"],
        "properties": {
          "status": {
            "type": "string",
            "enum": ["updated", "not_found", "not_supported", "no_change"],
            "description": "Outcome of the update operation. 'updated' = the memory was mutated. 'no_change' = request was well-formed but produced no observable change (e.g. content and metadata both omitted, or metadata merge resulted in an identical bag). 'not_found' = id does not exist under the active scope. 'not_supported' = backend does not implement mutation."
          },
          "id": {
            "type": "string",
            "description": "Echoed memory id. Present on status='updated' and 'no_change' so callers can correlate batched updates with their inputs."
          }
        }
      }
    },
    "amp.stats": {
      "description": "Return backend statistics for an agent namespace.",
      "annotations": {
        "title": "Backend Statistics",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "input": {
        "type": "object",
        "anyOf": [
          { "required": ["scope"] },
          { "required": ["agent_id"] }
        ],
        "properties": {
          "scope": {
            "$ref": "#/definitions/Scope"
          },
          "agent_id": {
            "type": "string",
            "description": "Legacy namespace identifier for the calling agent (mapped to scope.agent_id)."
          }
        },
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "required": ["memory_count"],
        "properties": {
          "memory_count": {
            "type": "integer",
            "minimum": 0,
            "description": "Total number of active and pinned memories."
          },
          "unconsolidated_count": {
            "type": "integer",
            "minimum": 0,
            "description": "Number of memories not yet consolidated. Optional for Core backends."
          },
          "metadata": {
            "type": "object",
            "description": "Backend-specific statistics. Supports reserved AMP keys.",
            "additionalProperties": true
          }
        }
      }
    },
    "amp.export": {
      "description": "Stream memories for a scope as Memory Exchange Format (MXF). Harness-only system API. See spec Appendix A.",
      "annotations": {
        "title": "Export Memories (MXF)",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "input": {
        "type": "object",
        "anyOf": [
          { "required": ["scope"] },
          { "required": ["agent_id"] }
        ],
        "properties": {
          "scope": {
            "$ref": "#/definitions/Scope"
          },
          "agent_id": {
            "type": "string",
            "description": "Legacy namespace identifier for the calling agent (mapped to scope.agent_id)."
          },
          "filters": {
            "$ref": "#/definitions/RecallFilters"
          },
          "page_size": {
            "type": "integer",
            "minimum": 1,
            "description": "Hint for the maximum number of memories to return in this page. Backends MAY emit fewer rows under memory pressure but MUST NOT exceed the hint. If absent on the MCP channel, the backend applies its own cap and uses next_cursor for continuation."
          },
          "cursor": {
            "type": "string",
            "description": "Opaque continuation token returned by a prior amp.export call. When omitted, the export starts from the beginning of the deterministic order."
          }
        },
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "required": ["count"],
        "oneOf": [
          {
            "description": "Synchronous export — the page is materialised inline. `ndjson` MUST be present (MAY be the empty string when count=0).",
            "required": ["ndjson"],
            "not": { "required": ["event_id"] }
          },
          {
            "description": "Async export — the backend has queued the operation. `event_id` MUST be present so the caller can poll/subscribe for the eventual MXF document.",
            "required": ["event_id"],
            "not": { "required": ["ndjson"] }
          }
        ],
        "properties": {
          "ndjson": {
            "type": "string",
            "description": "MXF document (NDJSON, one MemoryResult per line). Returned by the MCP adapter channel as a single string. The REST channel SHOULD stream application/x-ndjson instead. MUST be present (possibly empty) on a synchronous response; MUST be absent on an asynchronous response (paired with event_id)."
          },
          "count": {
            "type": "integer",
            "minimum": 0,
            "description": "Number of MemoryResult rows emitted in this page. On an async response this is the number of rows queued for emission (typically 0 until the event resolves)."
          },
          "next_cursor": {
            "type": "string",
            "description": "Opaque continuation token. When present and non-empty, the caller MUST issue another amp.export call passing this value in cursor to fetch the next page. When absent or empty, the export is complete."
          },
          "event_id": {
            "type": "string",
            "description": "Async processing reference. MUST be present when the backend executes the export asynchronously (paired with no ndjson). Callers poll/subscribe to this id to receive the eventual MXF document."
          }
        }
      }
    },
    "amp.import": {
      "description": "Ingest memories from Memory Exchange Format (MXF). Harness-only system API. See spec Appendix A.",
      "annotations": {
        "title": "Import Memories (MXF)",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "input": {
        "type": "object",
        "required": ["ndjson"],
        "anyOf": [
          { "required": ["scope"] },
          { "required": ["agent_id"] }
        ],
        "properties": {
          "scope": {
            "$ref": "#/definitions/Scope"
          },
          "agent_id": {
            "type": "string",
            "description": "Legacy namespace identifier (mapped to scope.agent_id)."
          },
          "ndjson": {
            "type": "string",
            "description": "MXF document (NDJSON, one MemoryResult per line). Each row MUST conform to the MemoryResult schema."
          },
          "on_conflict": {
            "type": "string",
            "enum": ["skip", "overwrite", "fail_atomic", "fail_fast"],
            "default": "skip",
            "description": "Policy controlling what happens when a row cannot be cleanly imported. For 'skip' and 'overwrite' the trigger is specifically an id collision (skip = keep existing and increment skipped count; overwrite = replace). For 'fail_atomic' and 'fail_fast' the trigger is ANY row-level failure — id collision, malformed JSON, MemoryResult schema validation failure, or scope rule violation: fail_atomic aborts and rolls back the entire import on the first such failure (backends without transaction support MUST return not_supported); fail_fast aborts on the first failure but DOES NOT roll back already-imported rows (caller resumes via id-keyed diff). The legacy value 'fail' is removed in favor of these two explicit modes."
          },
          "scope_remap": {
            "type": "string",
            "enum": ["strict", "inherit"],
            "default": "strict",
            "description": "Policy when an imported row's own scope omits an identity key present in the top-level request scope. strict (default) = the row is counted as failed with amp_error_code=invalid_request (preserves MXF round-trip semantics, prevents silent scope elevation). inherit = missing identity keys are inherited from the request scope (must be explicitly opted into; changes data ownership). In BOTH modes, rows whose scope CONFLICTS with the request scope on any key are always counted as failed."
          }
        },
        "additionalProperties": false
      },
      "output": {
        "type": "object",
        "required": ["imported", "skipped", "failed"],
        "properties": {
          "imported": {
            "type": "integer",
            "minimum": 0,
            "description": "Rows successfully stored as new or overwritten memories."
          },
          "skipped": {
            "type": "integer",
            "minimum": 0,
            "description": "Rows skipped due to existing id conflict (only when on_conflict='skip')."
          },
          "failed": {
            "type": "integer",
            "minimum": 0,
            "description": "Rows that failed validation or could not be stored."
          },
          "errors": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["line", "amp_error_code", "message"],
              "properties": {
                "line": {
                  "type": "integer",
                  "minimum": 1,
                  "description": "1-indexed line number of the failed row in the input NDJSON."
                },
                "amp_error_code": {
                  "type": "string",
                  "enum": ["invalid_request", "backend_error"]
                },
                "message": {
                  "type": "string"
                }
              }
            },
            "description": "Per-row failure details. Bounded length; backends MAY truncate."
          },
          "event_id": {
            "type": "string",
            "description": "Optional async processing reference if the backend executes the import asynchronously."
          }
        }
      }
    }
  }
}
